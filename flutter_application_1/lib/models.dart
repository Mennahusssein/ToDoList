class tasks {
  String id;
  String title;
  String description;
  bool isCompleted;
  tasks({
    required this.id,
    required this.title,
    required this.description,
    required this.isCompleted,
  });
}
